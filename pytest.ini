[pytest]
testpaths = tests
addopts = -ra
markers =
    balance: balance-analysis contract and metric tests
    smoke: lightweight end-to-end health checks
    integration: multi-component or persistence tests
