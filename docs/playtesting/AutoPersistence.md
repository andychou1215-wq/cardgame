# Auto Persistence — M2.4

`AutoPersistGuard` prevents Streamlit reruns from saving one finished game twice.

M2.4 also adds:
- rules-version comparison
- first-player win rate by version
- balance report export

A replay is still event-based, not deterministic action re-simulation.
